# recipe (P3)
Baseline: textarea -> `api.recipe({text})` -> coverage, per-line status (matched / fuzzy / grok_mapped / unmatched), neighbors.

TODO
- [x] add the recipe as a temporary glowing star at `response.xyz`
- [x] optional format/course pickers (RecipeRequest.format / course)
- [x] color line statuses; explain "grok_mapped" (Grok chose from our ingredient list)
