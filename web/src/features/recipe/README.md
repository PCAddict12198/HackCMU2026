# recipe (P3)
Baseline: textarea -> `api.recipe({text})` -> coverage, per-line status (matched / fuzzy / grok_mapped / unmatched), neighbors.

TODO
- [ ] add the recipe as a temporary glowing star at `response.xyz`
- [ ] optional format/course pickers (RecipeRequest.format / course)
- [ ] color line statuses; explain "grok_mapped" (Grok chose from our ingredient list)
