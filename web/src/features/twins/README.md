# twins (P3)
Baseline: `api.twins(selectedId)` -> cards with percentile, shared dims, diffs, and a "Why?" button (sets explainPair).

TODO
- [ ] side-by-side radar chart (source vs twin vectors from `space.dishes`)
- [ ] "fly to twin" and highlight the pair in the galaxy (`twinHighlight`)
- [ ] show `relaxation_note` subtly when `relaxation_level > 0`
- [ ] copy: "closer than N% of dish pairs"; never "N% similar"
