# twins (P3)
Baseline: `api.twins(selectedId)` -> cards with percentile, shared dims, diffs, and a "Why?" button (sets explainPair).

TODO
- [x] side-by-side radar chart (source vs twin vectors from `space.dishes`)
- [x] "fly to twin" and highlight the pair in the galaxy (`twinHighlight`)
- [x] show `relaxation_note` subtly when `relaxation_level > 0`
- [x] copy: "closer than N% of dish pairs"; never "N% similar"
