# shift (P3)
Baseline: six sigma-unit sliders (-2..2) -> debounced `api.shift` -> results list; target drawn by Galaxy.

TODO
- [ ] smooth target glide + trail
- [ ] "HOW?" card: `target = z_ramen - 0.8 e_rich + 1.2 e_sour` from shiftDeltas
- [ ] show `moved` per result (e.g. "sour +0.31")
- [ ] demo keyboard shortcuts (preset deltas)
