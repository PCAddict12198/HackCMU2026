# explain (P3)
Baseline: `api.explain(a, b)` -> per-dim table with share of difference + top attributions of the strongest dim.

TODO
- [ ] radar chart (a vs b)
- [ ] attribution bars per dim for both dishes ("Pork broth +0.31") with process tags
- [ ] provenance chips by `src` (usda / scoville / team / grok_reviewed / seed_placeholder), flagging seed values
