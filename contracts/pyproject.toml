# OWNER: integrator (main only). This package is the single source of truth for every interface.
[project]
name = "tastespace-contracts"
version = "1.0.0"
requires-python = ">=3.12"
dependencies = ["pydantic>=2.8", "pyyaml>=6.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/tastespace_contracts"]
